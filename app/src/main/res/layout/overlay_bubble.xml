<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/overlay_root"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content">

    <!-- 1. DOCKABLE ISLAND LAYOUT: Horizontal center-aligned narrow pill -->
    <RelativeLayout
        android:id="@+id/dockable_island_wrapper"
        android:layout_width="320dp"
        android:layout_height="46dp"
        android:layout_gravity="center"
        android:visibility="gone"
        android:clipChildren="false">
        
        <View
            android:id="@+id/dockable_island_bg"
            android:layout_width="130dp"
            android:layout_height="match_parent"
            android:layout_centerInParent="true" />
            
        <RelativeLayout
            android:id="@+id/dockable_island_container"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_centerInParent="true">

            <!-- Play/Pause Icon -->
        <ImageView
            android:id="@+id/island_btn_play_pause"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:layout_alignParentStart="true"
            android:layout_centerVertical="true"
            android:layout_marginStart="20dp"
            android:background="?android:attr/selectableItemBackgroundBorderless"
            android:clickable="true"
            android:focusable="true"
            android:src="@drawable/ic_play_symbol"
            android:visibility="gone"
            android:alpha="0"
            android:contentDescription="Play/Pause" />

        <!-- Reset Icon -->
        <ImageView
            android:id="@+id/island_btn_reset"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:layout_toEndOf="@id/island_btn_play_pause"
            android:layout_centerVertical="true"
            android:layout_marginStart="16dp"
            android:background="?android:attr/selectableItemBackgroundBorderless"
            android:clickable="true"
            android:focusable="true"
            android:src="@drawable/ic_reset_symbol"
            android:visibility="gone"
            android:alpha="0"
            android:contentDescription="Reset" />

        <!-- Center Time Text Display (Always Visible) -->
        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:gravity="center_vertical"
            android:orientation="horizontal">
            
            <ImageView
                android:id="@+id/island_timer_icon"
                android:layout_width="16dp"
                android:layout_height="16dp"
                android:layout_marginEnd="6dp"
                android:src="@drawable/ic_timer_island"
                android:tint="#FFFFFF"
                android:alpha="0.8" />
                
            <TextView
                android:id="@+id/island_time_text"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:fontFamily="sans-serif-medium"
                android:text="00:00"
                android:textColor="#FFFFFF"
                android:textSize="17sp"
                android:textStyle="bold"
                android:letterSpacing="0.04" />
        </LinearLayout>

        <!-- Close Icon -->
        <ImageView
            android:id="@+id/island_btn_close"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:layout_alignParentEnd="true"
            android:layout_centerVertical="true"
            android:layout_marginEnd="20dp"
            android:background="?android:attr/selectableItemBackgroundBorderless"
            android:clickable="true"
            android:focusable="true"
            android:src="@drawable/ic_close_symbol"
            android:visibility="gone"
            android:alpha="0"
            android:contentDescription="Close" />
        </RelativeLayout>
    </RelativeLayout>

    <!-- 2. FLOATING BUBBLE COLLAPSED VIEW -->
    <LinearLayout
        android:id="@+id/collapsed_container"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:orientation="horizontal"
        android:paddingHorizontal="16dp"
        android:paddingVertical="10dp"
        android:visibility="visible">

        <!-- Glowing active dot -->
        <View
            android:id="@+id/collapsed_glowing_dot"
            android:layout_width="8dp"
            android:layout_height="8dp"
            android:layout_gravity="center_vertical"
            android:layout_marginEnd="8dp"
            android:background="@drawable/active_dot_bg" />

        <TextView
            android:id="@+id/collapsed_time_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:fontFamily="sans-serif-medium"
            android:text="00:00"
            android:textColor="#FFFFFF"
            android:textSize="15sp"
            android:textStyle="bold" />
    </LinearLayout>

    <!-- 3. FLOATING BUBBLE EXPANDED VIEW -->
    <LinearLayout
        android:id="@+id/expanded_container"
        android:layout_width="230dp"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="14dp"
        android:visibility="gone">

        <!-- Top Header Info row -->
        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="4dp">

            <TextView
                android:id="@+id/expanded_title"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_alignParentStart="true"
                android:layout_centerInParent="true"
                android:fontFamily="sans-serif-medium"
                android:letterSpacing="0.1"
                android:text="ACTIVE TIMER"
                android:textColor="#FFFFFF"
                android:textSize="11sp" />

            <ImageView
                android:id="@+id/btn_close_overlay"
                android:layout_width="20dp"
                android:layout_height="20dp"
                android:layout_alignParentEnd="true"
                android:background="?android:attr/selectableItemBackgroundBorderless"
                android:clickable="true"
                android:focusable="true"
                android:src="@android:drawable/ic_menu_close_clear_cancel" />
        </RelativeLayout>

        <!-- Dynamic Timing Value Display -->
        <TextView
            android:id="@+id/expanded_time_text"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal"
            android:layout_marginTop="2dp"
            android:layout_marginBottom="12dp"
            android:fontFamily="sans-serif-thin"
            android:gravity="center"
            android:text="00:00"
            android:textColor="#FFFFFF"
            android:textSize="28sp" />

        <!-- Control Actions Row -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:orientation="horizontal">

            <TextView
                android:id="@+id/btn_overlay_pause_play"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginEnd="6dp"
                android:layout_weight="1"
                android:clickable="true"
                android:focusable="true"
                android:gravity="center"
                android:paddingVertical="6dp"
                android:text="Pause"
                android:textColor="#FFFFFF"
                android:textSize="12sp" />

            <TextView
                android:id="@+id/btn_overlay_add_time"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginEnd="6dp"
                android:layout_weight="1"
                android:clickable="true"
                android:focusable="true"
                android:gravity="center"
                android:paddingVertical="6dp"
                android:text="+1m"
                android:textColor="#FFFFFF"
                android:textSize="12sp" />

            <TextView
                android:id="@+id/btn_overlay_reset"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:clickable="true"
                android:focusable="true"
                android:gravity="center"
                android:paddingVertical="6dp"
                android:text="Reset"
                android:textColor="#FFFFFF"
                android:textSize="12sp" />
        </LinearLayout>
    </LinearLayout>
</FrameLayout>
